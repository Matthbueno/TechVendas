<UserControl x:Class="TechVendas.Views.PedidoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="700"
             Background="White">

    <Grid Margin="15" IsEnabled="{Binding PodeEditar}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Seleção de Client -->
            <RowDefinition Height="Auto"/>
            <!-- Adição de Produtos -->
            <RowDefinition Height="*"/>
            <!-- Grid Itens do Pedido -->
            <RowDefinition Height="Auto"/>
            <!-- Totais e Finalização -->
        </Grid.RowDefinitions>

        <!-- Seleção de Cliente -->
        <GroupBox Header="Dados do Cliente" Grid.Row="0" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Selecionar Cliente:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <ComboBox Width="350" ItemsSource="{Binding ClientsDisponiveis}" 
                          SelectedItem="{Binding ClientSelecionado}" DisplayMemberPath="Nome"/>
            </StackPanel>
        </GroupBox>

        <!-- Adição de Produtos -->
        <GroupBox Header="Adicionar Itens ao Pedido" Grid.Row="1" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Produto:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="250" ItemsSource="{Binding ProdutosDisponiveis}" 
                          SelectedItem="{Binding ProdutoSelecionado}" DisplayMemberPath="Nome" Margin="0,0,15,0"/>

                <TextBlock Text="Qtd:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="60" Text="{Binding QuantidadeInformada}" Margin="0,0,15,0" TextAlignment="Center"/>

                <Button Content="Adicionar Item" Command="{Binding AdicionarProdutoCommand}" Width="130" Background="#DDEBF7"/>
            </StackPanel>
        </GroupBox>

        <!-- Grid de Itens Inseridos -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding ItensDoPedido}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10"
                  HeadersVisibility="Column" AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Produto.Codigo}" Width="80"/>
                <DataGridTextColumn Header="Descrição do Produto" Binding="{Binding Produto.Nome}" Width="*"/>
                <DataGridTextColumn Header="Vlr. Unitário" Binding="{Binding Produto.Valor, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Qtd" Binding="{Binding Quantidade}" Width="60"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="120" FontWeight="SemiBold"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Rodapé: Pagamento e Total -->
        <Border Grid.Row="3" Background="#F8F9FA" Padding="15" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Forma de Pagamento:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ComboBox Width="150" SelectedItem="{Binding FormaPagamentoSelecionada}">
                        <ComboBoxItem Content="Dinheiro"/>
                        <ComboBoxItem Content="Cartão"/>
                        <ComboBoxItem Content="Boleto"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total do Pedido:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding ValorTotalPedido, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,25,0" VerticalAlignment="Center"/>

                    <Button Content="FINALIZAR PEDIDO" Command="{Binding FinalizarPedidoCommand}" Width="150" Height="40" Background="#1565C0" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>