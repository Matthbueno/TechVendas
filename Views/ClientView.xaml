<UserControl x:Class="TechVendas.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filtros Client -->
            <RowDefinition Height="*"/>
            <!-- Grid Clients -->
            <RowDefinition Height="Auto"/>
            <!-- Ações Client -->
            <RowDefinition Height="Auto"/>
            <!-- Filtros Pedidos do Client -->
            <RowDefinition Height="*"/>
            <!-- Grid Pedidos -->
        </Grid.RowDefinitions>

        <!-- Filtros de Busca -->
        <GroupBox Header="Buscar Clientes" Grid.Row="0" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="200" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0"/>

                <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="120" Text="{Binding FiltroCpf, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </GroupBox>

        <!-- Grid de Clientes -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Clients}" SelectedItem="{Binding ClientSelecionado}" 
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10"
                  HeadersVisibility="Column" AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome do Cliente" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,20">
            <!-- Botões de Ação do Cliente -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="left">
                <Button Content="Incluir Novo" Command="{Binding IncluirCommand}" Width="100" Margin="5"/>
                <Button Content="Editar" Command="{Binding EditarCommand}" Width="80" Margin="5"/>
                <Button Content="Salvar" Command="{Binding SalvarCommand}" Width="80" Margin="5" Background="#E2F0D9" Foreground="Black"/>
                <Button Content="Excluir" Command="{Binding ExcluirCommand}" Width="80" Margin="5" Background="#FCE4D6" Foreground="Black"/>
                <Button Content="Abrir Novo Pedido" Command="{Binding AbrirPedidoCommand}" Width="130" Margin="20,0,5,0" Background="#DDEBF7" Foreground="Black" FontWeight="Bold"/>
            </StackPanel>

            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="ID:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding ClientSelecionado.Id}" Width="40" IsReadOnly="True" Margin="5"/>
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding ClientSelecionado.Nome, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
                <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding ClientSelecionado.CPF, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5"/>
            </StackPanel>


        </StackPanel>

        <!-- Filtros do Histórico de Pedidos -->
        <GroupBox Header="Histórico de Pedidos do Cliente Selecionado" Grid.Row="3" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Pendentes" IsChecked="{Binding FiltroPedidosPendentes}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <CheckBox Content="Pagos" IsChecked="{Binding FiltroPedidosPagos}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <CheckBox Content="Entregues (Enviado/Recebido)" IsChecked="{Binding FiltroPedidosEntregues}" VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- Grid de Pedidos do Cliente -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding PedidosDoClient}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DataDaVenda, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Pagamento" Binding="{Binding FormaPagamento}" Width="110"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>

                <DataGridTemplateColumn Header="Atualizar Status" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Pago" Command="{Binding DataContext.MarcarPagoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2,0" Padding="5,0"/>
                                <Button Content="Enviado" Command="{Binding DataContext.MarcarEnviadoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2,0" Padding="5,0"/>
                                <Button Content="Recebido" Command="{Binding DataContext.MarcarRecebidoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2,0" Padding="5,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>